
server:
  port: 8004


spring:
  application:
    name: userService
  datasource:
    url: jdbc:mysql://localhost:3306/utilisateurdb?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Bengo1896*
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    format-sql: truespring.application.name=BankAccount



